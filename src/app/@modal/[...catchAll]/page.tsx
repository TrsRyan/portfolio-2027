// Soft navigation to any other route while a modal is open (e.g. Prev/Next
// to /another-slug): empties the @modal slot instead of leaving it displayed.
// Official Next pattern (Parallel Routes > Modals docs).
export default function ModalSlotCatchAll() {
  return null;
}
